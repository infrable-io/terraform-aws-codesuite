version: 0.2

run-as: root
phases:
  install:
    commands:
      - /bin/true
  pre_build:
    commands:
      - echo "Assuming cross-account CodeBuild role..."
      - $(assume-role $PROFILE)
  build:
    commands:
      - echo "Deploying to $PROFILE..."
